#include <stdio.h>
#include <math.h>

void main()
{
	float x1, y1, x2, y2, r1, r2;
	scanf("%f,%f,%f,%f,%f,%f", &x1, &y1, &r1, &x2 &y2 &r2);
	if ((x1 == x2) && (y1 == y2)) {
		if (r1 == r2) {

			print f("Окружности совпалают");
		}
		else if (r1 > r2) {
			print f("Вторая окружность внутри первой");
		}
	
		else if (r1 < r2) {
			print f("Первая окружность внутри Второй");
		}
	}
	else if ((x1 != x2) && (y1 != y2)) {
		if ((x1+r1)!=())
	}
}